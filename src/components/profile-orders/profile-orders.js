import React from "react";

import styles from './profile-orders.module.css';

export const ProfileOrders = () => {
    return (<div className={styles.main}>
        <h2 className="text text_type_main-large">Заказы</h2>
    </div>
    );
}